---
import DarkModeToggle from "../components/DarkModeToggle.astro";
import MainNavbar from "../components/MainNavbar.astro";
import MapSidebar from "../components/MapSidebar.astro";
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />

		<!-- Theme initialization script - must run before page renders -->
		<script is:inline>
			// Immediately set theme to prevent FOUC (Flash of Unstyled Content)
			(function() {
				const storedTheme = localStorage.getItem("theme");
				const prefersDarkMode = window.matchMedia("(prefers-color-scheme: dark)").matches;

				if (storedTheme === "dark" || (!storedTheme && prefersDarkMode)) {
					document.documentElement.classList.add("dark");
				}
			})();
		</script>

		<!-- Flowbite -->
		<link href="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.css" rel="stylesheet" />

		<title>Community Lens Database</title>
	</head>
	<body>
		<div class="antialiased bg-gray-50 dark:bg-gray-900">
			<MainNavbar />
			<MapSidebar />

			<!-- #######  Bring in the Components ####### -->
			<div class="flex">
				<div class="w-64 flex-none"></div><!-- creates space for the sidebar -->
				<div class="flex-auto">
					<slot />
				</div>
			</div>
		</div>
	<!-- Flowbite JS -->
	<script src="https://cdn.jsdelivr.net/npm/flowbite@3.1.2/dist/flowbite.min.js"></script>
	</body>


</html>


